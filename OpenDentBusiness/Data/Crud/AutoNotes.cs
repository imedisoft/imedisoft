//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//     Runtime Version: v4.0.30319
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using Imedisoft.Data.Models;
using MySql.Data.MySqlClient;
using System;
using System.Collections.Generic;

namespace Imedisoft.Data
{
	public partial class AutoNotes
	{
		public static AutoNote FromReader(MySqlDataReader dataReader)
		{
			return new AutoNote
			{
				Id = (long)dataReader["id"],
				AutoNoteCategoryId = dataReader["auto_note_category_id"] as long?,
				Name = (string)dataReader["name"],
				Content = (string)dataReader["content"]
			};
		}

		/// <summary>
		/// Selects a single AutoNote object from the database using the specified SQL command.
		/// </summary>
		/// <param name="command">The SELECT command to execute.</param>
		/// <param name="parameters">The (optional) command parameters.</param>
		public static AutoNote SelectOne(string command, params MySqlParameter[] parameters)
			=> Database.SelectOne(command, FromReader, parameters);

		/// <summary>
		/// Selects the <see cref="AutoNote"/> object with the specified key from the database.
		/// </summary>
		/// <param name="id">The primary key of the <see cref="AutoNote"/> to select.</param>
		public static AutoNote SelectOne(long id)
			=> SelectOne("SELECT * FROM `auto_notes` WHERE `id` = " + id);

		/// <summary>
		/// Selects multiple <see cref="AutoNote"/> objects from the database using the specified SQL command.
		/// </summary>
		/// <param name="command">The SELECT command to execute.</param>
		/// <param name="parameters">The (optional) command parameters.</param>
		public static IEnumerable<AutoNote> SelectMany(string command, params MySqlParameter[] parameters)
			=> Database.SelectMany(command, FromReader, parameters);

		/// <summary>
		/// Inserts the specified <see cref="AutoNote"/> into the database.
		/// </summary>
		/// <param name="autoNote">The <see cref="AutoNote"/> to insert into the database.</param>
		private static long ExecuteInsert(AutoNote autoNote)
			=> autoNote.Id = Database.ExecuteInsert(
				"INSERT INTO `auto_notes` " +
				"(`auto_note_category_id`, `name`, `content`) " +
				"VALUES (" +
					"@auto_note_category_id, @name, @content" +
				")",
					new MySqlParameter("auto_note_category_id", (autoNote.AutoNoteCategoryId.HasValue ? (object)autoNote.AutoNoteCategoryId.Value : DBNull.Value)),
					new MySqlParameter("name", autoNote.Name ?? ""),
					new MySqlParameter("content", autoNote.Content ?? ""));

		/// <summary>
		/// Updates the specified <see cref="AutoNote"/> in the database.
		/// </summary>
		/// <param name="autoNote">The <see cref="AutoNote"/> to update.</param>
		private static void ExecuteUpdate(AutoNote autoNote)
			=> Database.ExecuteNonQuery(
				"UPDATE `auto_notes` SET " +
					"`auto_note_category_id` = @auto_note_category_id, " +
					"`name` = @name, " +
					"`content` = @content " +
				"WHERE `id` = @id",
					new MySqlParameter("id", autoNote.Id),
					new MySqlParameter("auto_note_category_id", (autoNote.AutoNoteCategoryId.HasValue ? (object)autoNote.AutoNoteCategoryId.Value : DBNull.Value)),
					new MySqlParameter("name", autoNote.Name ?? ""),
					new MySqlParameter("content", autoNote.Content ?? ""));

		/// <summary>
		/// Deletes a single <see cref="AutoNote"/> object from the database.
		/// </summary>
		/// <param name="id">The primary key of the <see cref="AutoNote"/> to delete.</param>
		private static void ExecuteDelete(long id)
			 => Database.ExecuteNonQuery("DELETE FROM `auto_notes` WHERE `id` = " + id);

		/// <summary>
		/// Deletes the specified <see cref="AutoNote"/> object from the database.
		/// </summary>
		/// <param name="autoNote">The <see cref="AutoNote"/> to delete.</param>
		private static void ExecuteDelete(AutoNote autoNote)
			=> ExecuteDelete(autoNote.Id);
	}
}
